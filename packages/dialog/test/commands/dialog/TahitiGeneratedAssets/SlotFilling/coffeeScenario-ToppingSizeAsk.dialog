
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$ToppingSize",
    "priority": "indexOf(dialog.requiredProperties, 'ToppingSize')",
    "actions": [       
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskToppingSize()}",
            "expectedProperties": [
                "ToppingSize"
            ]
        }
    ]
}
