
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Topping",
    "priority": "indexOf(dialog.requiredProperties, 'Topping')",
    "actions": [       
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskTopping()}",
            "expectedProperties": [
                "Topping"
            ]
        }
    ]
}
