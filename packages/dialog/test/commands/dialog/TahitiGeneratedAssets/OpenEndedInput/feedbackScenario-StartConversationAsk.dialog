
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$StartConversation",
    "priority": "indexOf(dialog.requiredProperties, 'StartConversation')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskStartConversation()}",
            "expectedProperties": [
                "StartConversation"
            ]
        }
    ]
}
