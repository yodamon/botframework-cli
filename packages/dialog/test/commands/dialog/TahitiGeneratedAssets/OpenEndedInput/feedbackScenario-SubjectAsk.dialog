
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Subject",
    "priority": "indexOf(dialog.requiredProperties, 'Subject')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskSubject()}",
            "expectedProperties": [
                "Subject"
            ]
        }
    ]
}
