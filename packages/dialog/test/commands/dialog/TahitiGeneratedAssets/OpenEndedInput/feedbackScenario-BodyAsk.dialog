
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Body",
    "priority": "indexOf(dialog.requiredProperties, 'Body')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskBody()}",
            "expectedProperties": [
                "Body"
            ]
        }
    ]
}
