
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Value",
    "priority": "indexOf(dialog.requiredProperties, 'Value')",
    "actions": [        
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskValue()}",
            "expectedProperties": [
                "Value"
            ]
        }
    ]
}
