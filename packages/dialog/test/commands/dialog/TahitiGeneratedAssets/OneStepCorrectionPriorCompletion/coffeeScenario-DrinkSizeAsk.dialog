
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$DrinkSize",
    "priority": "indexOf(dialog.requiredProperties, 'DrinkSize')",
    "actions": [       
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskDrinkSize()}",
            "expectedProperties": [
                "DrinkSize"
            ]
        }
    ]
}
