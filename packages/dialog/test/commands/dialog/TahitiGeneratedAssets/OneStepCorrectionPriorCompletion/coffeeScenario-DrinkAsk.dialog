
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Drink",
    "priority": "indexOf(dialog.requiredProperties, 'Drink')",
    "actions": [       
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskDrink()}",
            "expectedProperties": [
                "Drink"
            ]
        }
    ]
}
